spring:
  application:
    name: scheduler

  datasource:
    name: scheduler
    admin-url: jdbc:postgresql://localhost:5432/postgres
    url: jdbc:postgresql://localhost:5432/${spring.datasource.name}
    username: postgres
    password: postgres

  flyway:
    enabled: false 
    clean-disabled: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  redis:
    host: localhost
    port: 6379
    password: paSSWord

  session:
    store-type: redis
    timeout: 86400
  
  file:
    image-dir: ./src/main/resources/image

  mail:
      host: smtp.gmail.com
      port: 587
      username: scheduler.app.noreply@gmail.com
      password: gmlc oylh goks mhqq
      properties:
        mail:
          smtp:
            auth: true
            starttls:
              enable: true
security:
  jwt: 
    expirationMs: 86400000 # 24 * 60 * 60 * 1000
    secret: ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/
